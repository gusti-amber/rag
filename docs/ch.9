# LangGraphの基礎
## LangGraphとは？
LangGraphは、LLMを活用した複雑なワークフローを開発するためのPythonライブラリです。
このライブラリの特徴はこのワークフローをグラフ構造注1としてモデル化する点にあります。
ここで言うグラフとは、私たちが日常的に目にする組織図（図9.1）や路線図（図9.2）のようなもので、ノード（頂点）とエッジ（辺）で構成される構造（図9.3）のことを指します。

LangGraphの特徴をさらに詳しく見ていくと、次の点が挙げられます。 

#### 1． 明示的なステート管理
LangGraphでは、システムの「ステート（状態）」を明示的に定義し、管理することができます。ステートは、会話の履歴、収集した情報、中間結果などを含む構造化されたデータとして表現されます。各ノードはこのステートを入力として受け取り、処理を行ったあと、ステートを更新します。これにより、長期的なタスクや複数のステップを要する処理において、一貫性のある情報の受け渡しと更新が可能になります。 

#### 2．条件分岐とループの自然な表現
グラフ構造を用いることで、「もし～なら」という条件分岐や、「～になるまで繰り返す」というループ処理を直感的に表現できます。これにより、複雑な意思決定プロセスを持つシステムの実装が容易になります。 

#### 3． 段階的な拡張性
新しい機能を追加したい場合、既存のグラフ構造に新しいノードを追加し、適切なエッジで接続するだけで済みます。これにより、システムの段階的な拡張が容易になります。 

#### 4． デバッグとテストの容易さ
各ノードを独立してテストできるため、システムのデバッグやテストが容易になります。また、LangSmithと組み合わせることによりグラフ単位での処理のトレースが可能になるため、問題が発生した箇所を特定しやすくなります。

#### 5． チェックポイントとリカバリ
LangGraphはステートのチェックポイントを作成し、保存する機能を提供します。これにより、長時間実行されるタスクを中断し、あとで再開したり、エラーが発生した場合に特定のポイントから処理を再開したりすることが可能になります。

## LangGraphの主要コンポーネント
それではLangGraphの主要コンポーネントについて見ていきましょう。ここではLangGraphの中心的な概念であるステート、ノード、エッジ、コンパイル済みグラフについて、コード例を交えて詳しく解説していきます。
